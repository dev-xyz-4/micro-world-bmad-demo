# Questions — p3-flow-contract-scaffold

## Resolved
- [x] The minimum required future BMAD artifact beyond `01-break.md` and `questions.md` is `04-deliver.md`. `02-model.md` and `03-analyze.md` are optional and should be created only if structural modeling or decision/trade-off analysis becomes necessary.
- [x] Later-phase outputs are feature-local by default under `docs/bmad/features/p3-flow-contract-scaffold/`, but bounded updates to existing planning continuity artifacts are allowed when required by execution state or closure continuity (especially the active Work-Unit Record and `.planning/MODELS/Phase4-Orchestrator/STATE.md`).
- [x] Helper-assisted later execution is allowed only within the existing approved surfaces and current Model-A boundary. No helper redesign, no silent argument inference, and no expansion of helper semantics is authorized by this slice.
- [x] No separate mode-aware history/handover writeback is required for this docs-only BMAD planning slice while the version decision remains `no SemVer change`. Normal active continuity updates still apply through the canonical Work-Unit Record and `STATE.md`; if later work introduces release/tag/history impact, routing must follow the canonical mode-aware targets from `docs/bmad/guides/CODEX_ENTRY.md`.

## Open / Blocking
- none

## Initial Questions (Historical State)
- [ ] Confirm the exact minimum artifact set the future `04-deliver.md` must require for this starter/scaffold (beyond `01-break.md` and `questions.md`).
- [ ] Confirm whether later-phase outputs for this first slice must stay strictly under `docs/bmad/features/p3-flow-contract-scaffold/` or may include bounded updates to existing planning continuity artifacts.
- [ ] Confirm whether helper-assisted generation is allowed in later phases for this slice, or if this slice must remain fully manual/document-first.
- [ ] Confirm whether any mode-aware continuity/handover writeback is required for this docs-only BMAD planning slice when version decision is `no SemVer change`.
