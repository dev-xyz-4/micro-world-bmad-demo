# Architecture Divergences

## Current Status

No critical architecture divergences are recorded for `v0.3.0`.

## Notes

- The planning sketch mentioned optional `features/` and `minors/` folders inside `micro-world-app/src/`.
- The released baseline does not create those folders yet.
- This is intentional deferral, not a conflicting implementation choice: the current demo only needs `core/`, `App.jsx`, and `App.css`.
